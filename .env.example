# Only used when running from source (npm start). Copy to .env and fill in. Never commit .env.
# The packaged app doesn't read this: enter your key under "API key…" in the app instead, which stores it encrypted.
# Values here override any same-named variable in your system environment.
TYPESAFE_API_KEY=
# PORT=3000
